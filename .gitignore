# Зависимости (папка node_modules)
node_modules/

# Логи и файлы отладки
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Переменные окружения (секреты)
.env
.env.local
.env.*.local

# Папки со сборкой (если используете Webpack, Vite и т.д.)
/dist
/build
/out

# IDE (Visual Studio Code)
.vscode/*
!.vscode/settings.json  # если нужно сохранить настройки workspace
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Редакторы (Sublime, Atom, etc.)
*.sublime-*
*.komodo-*
*.idea/
*.swp
*.swo
*~

# Системные файлы (macOS)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Тесты и временные файлы
coverage/
.nyc_output/
.temp/
.cache/

# Конфигурация Prettier / ESLint (опционально – если не хотите хранить их в репозитории)
# .prettierrc
# .eslintrc
# Но обычно их хранят, чтобы у всей команды были одинаковые настройки

# Директории для зависимостей (если используете bower)
bower_components/

# Прочее
*.pid
*.seed
*.pid.lock